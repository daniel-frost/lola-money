# Node 22.11 is a hair below what a couple of transitive dev-deps request (>=22.13).
# The gap is cosmetic for our purposes; relax the engine check so installs succeed.
# TODO: remove this once Node is bumped to the latest 22 LTS.
--ignore-engines true
